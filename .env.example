# copy this file to .env and fill in your details
# only needed if you run the pipeline with --db postgres or --db mysql
# .env is gitignored so your real credentials never get committed

DB_HOST=localhost
DB_PORT=5432
DB_NAME=jobmarket
DB_USER=your_user
DB_PASSWORD=your_password
DB_TABLE=jobs
